import React from 'react';

const AuthWrapper = () => {
  return (
    <div className="auth_wrapper">
      <button className="auth_btn-login">Log in</button>
      <button className="auth_btn-register">Sign up</button>
    </div>
  );
};

export default AuthWrapper;
